# PRD — 타로 앱 (가제)

작성일: 2026-09-08
작성자: 이지연

---

## 1. 무엇을 만드나

타로 카드를 뽑으면 AI가 해석해 주고, **로그인한 사용자가 자기가 본 이력을 다시 볼 수 있는** 웹 앱.

## 2. 왜 만드나

모두의연구소 AI 에이전트 과정 과제(갈래 B — 내 서비스에 백엔드 붙여 배포).
목표는 하나 — **백엔드가 실제로 도는 것**을 직접 경험하는 것.
제출 시 보여야 하는 장면: **새로고침해도, 다른 기기에서도 내 이력이 남아 있다.**

## 3. 사용자 시나리오

1. 사이트에 들어온다
2. 로그인한다
3. 질문을 적고 카드를 뽑는다
4. AI가 해석을 준다
5. 결과가 저장된다
6. **새로고침한다 → 이력에 그 결과가 그대로 있다**
7. 다른 기기에서 로그인해도 같은 이력이 보인다
8. 남의 이력은 보이지 않는다

6번과 8번이 이 과제의 핵심이다. 6번은 "백엔드가 돈다"의 증거이고, 8번은 "권한 확인(인가)"의 증거다.

## 4. 기능 범위

### 화면만으로 되는 것 (서버 필요 없음)

| 기능 | 왜 서버가 필요 없나 |
|---|---|
| 카드 섞기·뽑기 | 그 순간 계산하면 끝. 남길 필요 없음 |
| 카드 그림·뒤집기 애니메이션 | 브라우저가 그림 |
| 결과 화면 배치 | 받은 데이터를 보여주기만 함 |

### 백엔드가 필요한 것

| 기능 | 왜 필요한가 |
|---|---|
| 로그인 | 누구인지 서버가 알아야 함 |
| 결과 저장 | 새로고침·다른 기기에서도 남아야 함 |
| 내 이력 목록 | **내 것만** 보여야 함 → 권한 확인 필요 |
| AI 해석 호출 | API 키를 브라우저에 두면 안 됨 |

마지막 줄이 중요하다. AI 해석을 쓰기로 하면서 **API 키라는 비밀값이 생겼고**, 그 키는 페이지 소스를 열면 누구나 볼 수 있는 자리에 두면 안 된다. 남이 그 키로 AI를 호출하면 요금은 나에게 온다. 그래서 AI 호출은 반드시 서버를 거친다.

## 5. 설계 한 장

| 항목 | 내용 |
|---|---|
| 백엔드가 꼭 필요한 기능 | 로그인 · 이력 저장 · **내 이력만** 조회 · AI 해석 호출 |
| 화면만으로 되는 기능 | 카드 섞기·뽑기, 카드 그림·뒤집기, 결과 화면 |
| 필요한 창구 | 이력 목록(Read) · 결과 저장(Create) · 이력 삭제(Delete) · 해석 요청(Edge Function) |
| 다뤄야 할 비밀값 | `ANTHROPIC_API_KEY` (Edge Function 환경변수, 브라우저에 안 내려감)<br>`SUPABASE_ANON_KEY` (공개 키. 노출돼도 되지만 **RLS가 켜져 있어야만**) |
| 호스팅 | Supabase (Auth + DB + Edge Function) + GitHub Pages (화면) |

**호스팅을 이렇게 고른 이유 한 줄:**
백엔드가 저장·조회·로그인 위주라 BaaS가 가장 빠르고, AI 키를 숨길 자리(Edge Function)까지 같은 곳에 있어서 관리할 곳이 하나로 끝난다.

### 구조

```
브라우저 (GitHub Pages)
  ├─→ 로그인          →  Supabase Auth
  ├─→ 이력 저장/조회   →  Supabase DB   (RLS로 내 것만)
  └─→ 카드 해석 요청   →  Supabase Edge Function  →  Claude API
                          (여기에만 ANTHROPIC_API_KEY가 있음)
```

## 6. 데이터 모델

테이블 하나면 된다.

### `readings`

| 컬럼 | 타입 | 용도 |
|---|---|---|
| `id` | uuid | 기록 하나의 번호 |
| `user_id` | uuid | **누구 것인지.** RLS가 이 칸을 보고 거른다 |
| `created_at` | timestamptz | 언제 봤는지 |
| `question` | text | 무엇을 물었는지 |
| `cards` | jsonb | 뽑힌 카드들 |
| `interpretation` | text | AI가 준 해석 |

- 덱은 **전체 78장**(메이저 22 + 마이너 56)
- `cards` 의 구체적 형태는 [결정 필요] 2번(역방향)에 달려 있다

## 7. 창구 (엔드포인트)

| 창구 | CRUD | 하는 일 |
|---|---|---|
| 이력 목록 주기 | Read | 로그인한 사람의 기록만 최신순으로 |
| 결과 저장하기 | Create | 해석까지 다 나온 뒤 **한 번에** 저장 |
| 이력 지우기 | Delete | 내 기록 하나 삭제 |
| 해석 요청 | (Edge Function) | 뽑힌 카드를 받아 Claude API를 부르고 해석을 돌려줌 |

### Update가 없는 이유

저장을 **해석까지 다 받은 뒤 한 번에** 하기로 정했다(INSERT 하나). 그래서 나중에 고칠 일이 없다.

카드를 먼저 저장하고 해석을 나중에 채우는 방식이었다면 UPDATE가 필요했다. 그 방식을 택하지 않은 이유는, INSERT와 UPDATE 사이에 AI 호출이 끼면서 세 가지 실패 지점이 생기기 때문이다.

1. AI 호출 실패 (Edge Function 에러, Claude API 과부하 529·한도 초과 429, 타임아웃)
2. AI는 답했는데 UPDATE가 실패 (네트워크 끊김)
3. 사용자가 해석 도착 전에 창을 닫음

셋 다 결과가 같다 — 행은 있는데 `interpretation` 이 빈 채로 남는다. 그 행을 목록에서 어떻게 보여줄지, 재시도할지, 지울지를 전부 정해야 한다.

**대가:** 한 번에 저장하면 AI 호출이 실패했을 때 그 판은 이력에 안 남는다. 카드를 뽑았다는 기록도 남지 않는다. 이건 감수한다.

## 8. 보안

과제 제출 전 체크리스트에 맞춰 정리한다.

| 체크 항목 | 이 앱에서는 |
|---|---|
| 비밀값을 코드에 직접 안 적고 환경변수로 뺐는가 | `ANTHROPIC_API_KEY` 는 Edge Function 환경변수. 프론트 코드·저장소 어디에도 안 넣는다 |
| 검증을 백엔드에서 하는가 | 어느 기록이 누구 것인지의 판단을 **DB(RLS)** 가 한다. 프론트가 보내는 `user_id` 를 믿지 않는다 |
| RLS를 켜고 정책을 넣었는가 | `readings` 에 RLS ON. 정책은 아래 |
| 로그인이 필요한 데이터에 권한 확인이 들어가는가 | RLS가 곧 그 역할 |
| 무료 티어 한도와 요금 알림을 아는가 | Supabase 무료 티어 한도 확인 + Anthropic Console에서 사용량 한도·알림 설정 |

### RLS 정책

```sql
alter table readings enable row level security;

-- 내 것만 읽는다
create policy "read own" on readings
  for select using (auth.uid() = user_id);

-- 내 것으로만 저장한다
create policy "insert own" on readings
  for insert with check (auth.uid() = user_id);

-- 내 것만 지운다
create policy "delete own" on readings
  for delete using (auth.uid() = user_id);
```

RLS를 안 켜면 공개 키(`SUPABASE_ANON_KEY`)만으로 누구나 남의 점괘를 읽고 지울 수 있다.
실제로 이 문제로 수백 개 앱이 데이터를 노출한 사고가 있었다 (CVE-2025-48757).
**AI 코딩 도구로 만든 테이블은 RLS가 꺼진 채 시작되는 경우가 많으므로 반드시 확인한다.**

## 9. [결정 필요]

아직 정하지 않은 것들. 여기가 채워져야 구현에 들어간다.

### 1. 스프레드 — 한 번에 몇 장 뽑나
- 1장 (오늘의 카드)
- 3장 (과거 · 현재 · 미래)
- 켈틱크로스 10장

→ 카드 수가 정해져야 화면 배치와 AI에게 보낼 프롬프트가 정해진다.

### 2. 역방향 — 쓸 건가, 정방향만
- 쓴다 → `cards` 에 카드 이름 + 정/역 여부를 같이 저장
- 안 쓴다 → `cards` 에 카드 이름만

### 3. 로그인 방식
- 이메일 + 비밀번호
- 구글 로그인
- 매직링크 (메일로 온 링크 클릭)

### 4. 앱 이름과 폴더명
- `taro` 는 짝(jongmins0410-cmyk)의 저장소를 클론해둔 것이라 쓸 수 없다
- 로컬 폴더명과 GitHub 저장소명을 정해야 한다 (달라도 상관은 없다)

## 10. 이번 과제에서 안 하는 것

- 카드 그림 일러스트 제작 (텍스트·간단한 도형으로 대신)
- 이력 검색·필터
- 결과 공유 링크
- 결제·유료 기능

---

## 부록 — 준비물

지연님이 직접 해야 하는 것 (로그인·결제수단이 걸려 있어 대신 못 함).

1. **Supabase 가입** — https://supabase.com (GitHub 계정으로 로그인 가능, 무료)
2. **Claude API 키 발급** — https://console.anthropic.com
   - API Keys → Create Key
   - `sk-ant-` 로 시작하는 값이 **한 번만** 보인다. 그때 복사해 둘 것
   - 결제수단 등록 필요 (최소 $5)
   - Billing → Usage limits 에서 **한도와 알림을 걸어둘 것**
   - 이 키는 나중에 Supabase 대시보드에 직접 넣는다. 대화창에 붙여넣지 않는다
